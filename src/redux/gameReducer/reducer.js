const initialState = {
  playStatus: false,
  editingDrumPosition: false,
  isRecording: false,
};

export default (state = initialState, action) => {
  switch (action.type) {
    // start / stop play status
    default:
      return state;
  }
};
